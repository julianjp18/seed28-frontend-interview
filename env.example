# Copiar a .env.local y ajustar si es necesario
# URL del backend NestJS (Bulltrack Pro) - usada por la API route de Next.js en el servidor
API_URL=http://localhost:3001
# Opcional: si no existe API_URL, la API route usa esta variable
NEXT_PUBLIC_API_URL=http://localhost:3001
